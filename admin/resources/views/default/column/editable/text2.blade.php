<a href="#"
    class="inline-editable2"
    data-name="{{ $name }}"
    data-value="{{ $value }}"
    data-url="{{ $url }}"
    data-type="text"
    data-pk="{{ $id }}"
    style="text-align:right; color: green;"
>{{ $value }}</a>


{!! $append !!}